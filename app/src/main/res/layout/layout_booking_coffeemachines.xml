<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <!-- STORE INPUTS -->
    <LinearLayout
        android:id="@+id/linearLayout_coffeeMachines"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:fontFamily="@font/montserratt"
            android:padding="5dp"
            android:text="Enter your Coffee Machine's Serial Number. (Max 5)"
            android:textColor="#000"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:ignore="HardcodedText,UnusedAttribute" />

        <EditText
            android:id="@+id/input_serial"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginVertical="5dp"
            android:background="@drawable/custom_textbox"
            android:hint="Serial No 1"
            android:inputType="text"
            tools:ignore="Autofill,HardcodedText,TouchTargetSizeCheck" />
    </LinearLayout>

    <!-- ADD and PLUS BUTTONS -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/btn_remove_serial"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:layout_margin="5dp"
            android:foreground="?android:attr/selectableItemBackgroundBorderless"
            app:srcCompat="@drawable/icon_minus"
            tools:ignore="ContentDescription,UnusedAttribute,ImageContrastCheck" />

        <ImageView
            android:id="@+id/btn_add_serial"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center"
            android:layout_margin="5dp"
            android:foreground="?android:attr/selectableItemBackgroundBorderless"
            app:srcCompat="@drawable/icon_plus"
            tools:ignore="ContentDescription,UnusedAttribute,ImageContrastCheck" />
    </LinearLayout>

</LinearLayout>